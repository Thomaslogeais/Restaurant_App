# PostgreSQL connection string (Neon serverless — works in Cloudflare Workers)
# Create a free project at https://neon.tech and copy the connection string
DATABASE_URL=postgresql://user:password@ep-example-123456.us-east-1.aws.neon.tech/restaurant?sslmode=require
