/**
 * Demo constant — single-restaurant setup.
 * All API calls use this restaurantId as the required query parameter.
 */
export const RESTAURANT_ID = 1 as const;

// Expo Router treats every file in app/ as a potential route.
// Exporting a non-component default silences the "missing default export" warning.
export default {};
