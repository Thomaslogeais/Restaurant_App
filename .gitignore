# Dependencies
node_modules

# Turbo
.turbo

# Build outputs
dist
.expo
.next
out

# Cloudflare Workers local state (contains secrets — never commit)
.wrangler
.dev.vars

# Environment files with real secrets
.env.local
.env.*.local

# Editor
.vscode/settings.json
.idea

# OS
.DS_Store
Thumbs.db

# Generated API client files ARE committed for reviewer convenience.
# They are marked linguist-generated in .gitattributes.
# Re-generate with: pnpm gen:contract  (never hand-edit)

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
